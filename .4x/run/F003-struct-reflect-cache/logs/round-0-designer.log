[assistant] 我先看一下現有的程式碼結構與 Struct() 實作，以及是否有前一次 session 的產出。
[tool_use] Bash: ls -la *.go && echo "---RUN DIR---" && ls -la .4x/run/F003-struct-reflect-cache/ 2>/dev/null && echo "---REVIEW---" && c...
[tool_use] Read: /Users/white/github/go-masker/masker.go
[assistant] 我已理解現有 `Struct()` 實作。讓我快速確認 package-level 的對外函式與 `parseGenericMask`。
[tool_use] Bash: grep -n "^func Struct\|^func Marshal\|^func Format\|func parseGenericMask\|func.*Format" *.go | head -30
[tool_use] Bash: grep -rn "^func " *.go | grep -v "_test.go" | grep -iv "masker\b" | grep -E "^[a-z].*func (Struct|Marshal|Format|String)...
[assistant] 我已掌握足夠資訊。現有 API 全是 `*MaskerMarshaler` 的 method，沒有 package-level wrapper。`Format` 應設計為 `MaskerMarshaler` 的 method。讓我快速看 Struct 的既有測試模式。
[tool_use] Bash: grep -n "func Test\|func Benchmark\|m.Struct\|\.Struct(" masker_test.go | head -30; echo "---benchmark exist?---"; grep ...
[assistant] 已掌握全貌：所有 API 都是 `*MaskerMarshaler` 的 method，目前無 benchmark、無 package-level wrapper、無 `Format`。`Struct()` 每次都重讀 `Type().Field(i).Tag`。現在開始增量寫出三份產出。

先寫 task-brief.md：
[tool_use] Write: /Users/white/github/go-masker/.4x/run/F003-struct-reflect-cache/task-brief.md
[assistant] 現在寫 acceptance-criteria.md：
[tool_use] Write: /Users/white/github/go-masker/.4x/run/F003-struct-reflect-cache/acceptance-criteria.md
[assistant] 現在寫 test-strategy.yaml 與 selected-learnings.json：
[tool_use] Write: /Users/white/github/go-masker/.4x/run/F003-struct-reflect-cache/test-strategy.yaml
[tool_use] Write: /Users/white/github/go-masker/.4x/run/F003-struct-reflect-cache/selected-learnings.json
[assistant] 三份必要產出已寫入磁碟，並附上 selected-learnings。設計摘要：

## F003 Struct() reflect cache — 設計重點

**核心問題**：`masker.go:224-228` 每次呼叫 `Struct()` 都重讀 `selem.Type().Field(i).Tag.Get(tagName)` 解析 metadata，同 type 重複呼叫浪費 CPU。

**四個子任務的設計決策**：

1. **type-cache**：`var typeCache sync.Map`（key `reflect.Type` → `*typeInfo`），`fieldMask` 描述單欄位（index、`MaskerType`、kind 分類、遞迴旗標），`cachedTypeInfo` 走 `LoadOrStore`。額外要求一個**同 package、不匯出**的 `resetTypeCache()` 供 benchmark/測試用，避免污染 public API。

2. **struct-use-cache**：`Struct()` 改迭代 `typeInfo.fields`，**所有既有分支語意逐一保留**（向後相容是硬性前提）。

3. **format-func**：`Format(s) string` 走 `strings.Builder` 不建新 struct；確定性輸出（**不印記憶體位址**，與 `fmt %v` 對 pointer 印 `0x...` 區隔）；nil→`""` 不 panic；遮罩值與 `Struct()` byte-identical（parity）。

4. **benchmark**：`BenchmarkStructCold`（每輪 reset cache）vs `BenchmarkStruct`（warm）證明 ≥50% CPU 降幅；`BenchmarkFormat` + `testing.AllocsPerRun` 證明 Format allocs < Struct allocs。

**幾個關鍵的可驗證性設計**（避免 metric hack）：
- AC-1/AC-7 沿用既有 fixture 做 parity（採納 L003/L006）。
- AC-3 用「metadata 只建立一次」這個確定性指標，比直接斷言時間更可靠。
- AC-9 用 `testing.AllocsPerRun` 做確定性 alloc 比較，不靠浮動的 benchmark 時間。
- ≥50% CPU（AC-10）用 cold vs warm 兩條 benchmark 對比，而非無基準可比的單點數字。

**選用 learnings**：L001（行為保留型 feature 須明列契約不變）、L003 + L006（沿用既有測資做 parity）、L005（退化情況選最安全：nil→空字串）。

`Format()` 我設計為 `*MaskerMarshaler` 的 method 而非 package-level 函式，理由是現有 API（`Struct`/`Marshal`/`Get`…）全是 method，保持一致。若你預期 `Format` 應提供 package-level 捷徑，可在 review 時提出。
[result] success (205.4s, $1.1558)
