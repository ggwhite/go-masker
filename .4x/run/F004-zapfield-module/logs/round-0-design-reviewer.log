[assistant] 我先讀取三份輸入文件，以及相關的既有 v3 程式碼來評估設計。
[tool_use] Read: /Users/white/github/go-masker/.4x/run/F004-zapfield-module/task-brief.md
[tool_use] Read: /Users/white/github/go-masker/.4x/run/F004-zapfield-module/acceptance-criteria.md
[tool_use] Read: /Users/white/github/go-masker/.4x/run/F004-zapfield-module/test-strategy.yaml
[assistant] 讓我驗證 v3 core 既有 API 是否符合設計假設（package-level 函式簽名、Sensitive[T]、NewPhone、go.mod 的 go 版本）。
[tool_use] Bash: ls v3/ && echo "=== go.mod ===" && cat v3/go.mod && echo "=== zapfield exists? ===" && ls v3/zapfield 2>/dev/null || ech...
[tool_use] Bash: echo "=== convenience.go (package-level funcs) ===" && grep -E "^func (Mobile|Email|Password|Name|Address|ID|Credit|Tel|...
[tool_use] Bash: echo "=== String() impl ===" && sed -n '45,75p' v3/sensitive.go && echo "=== convenience Abuse/All ===" && grep -A3 "^fu...
[tool_use] Bash: ls $(go env GOMODCACHE)/go.uber.org/ 2>/dev/null | grep -i zap || echo "no zap in cache"; echo "---multierr/atomic (zap ...
[assistant] 所有設計假設皆驗證通過：v3 core 的 12 個 package-level 函式（簽名均 `func(string) string`）、`Sensitive[T].String()`（回傳快取 `masked`，zero-value 為 `""`）、`NewPhone` 等建構子都存在；`go.uber.org/zap v1.28.0` 及其依賴（multierr、atomic）已在 module cache，offline build 可行。我來寫 review report。
[tool_use] Write: /Users/white/github/go-masker/.4x/run/F004-zapfield-module/design-review-report.md
[assistant] 設計審查完成，判定 **PASS**。

## 審查結論

F004 zapfield 的設計可直接進入 coding，三份產出（task-brief、acceptance-criteria、test-strategy）一致且完整。我實際驗證了所有對 v3 core 的 API 假設都成立：

**已驗證的關鍵事實**
- v3 core 的 12 個 package-level 函式（`Mobile`、`Email`、`Password`…`None`、`All`）都存在，簽名均為 `func(string) string`，可被 `func(key, value string) zap.Field` 完整包裝。
- `Sensitive[T].String()` 回傳快取的 `masked` 欄位（絕不碰 raw），zero-value 為 `""` → 符合 AC-8 的安全退化斷言。
- `go.uber.org/zap v1.28.0` 及其依賴（multierr、atomic）已在 module cache，offline build/tidy 可行。

**兩條 feature 紅線都有守住**
- 依賴隔離：`replace => ../` 路徑正確，zap 只進 `zapfield/go.mod`；巢狀 module 讓 `cd v3 && go test` 不會污染 core（AC-2、AC-11）。
- 禁 reflection：強制 `zap.String` / `zapcore.StringType`，並用 `! grep zap.Any|zap.Reflect` 機檢（AC-6）。

**僅兩個非阻擋性小瑕疵**（coder 順手修即可，不退回）
1. AC-8／task-brief 描述 zero-value 用「mask 為 nil」，實際是 `masked string` 欄位的 `""`，行為斷言正確、措辭可精準化。
2. verify_commands 未列 `go mod tidy`，但 task-brief 1.1 已要求且依賴在 cache，無實質風險。

報告已寫入 `/Users/white/github/go-masker/.4x/run/F004-zapfield-module/design-review-report.md`。
[result] success (102.8s, $0.7257)
