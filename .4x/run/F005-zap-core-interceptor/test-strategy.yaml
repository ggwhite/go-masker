web: false
api: false
coder_only: true
profiles:
  - unit
verify_commands:
  - "cd v3/zapfield && go build ./..."
  - "cd v3/zapfield && go vet ./..."
  - "cd v3/zapfield && go test -race -cover ./..."
